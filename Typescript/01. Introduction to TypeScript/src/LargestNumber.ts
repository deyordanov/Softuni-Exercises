function getLargestNumber(first: number, second: number, third: number): void {
  const largestNumber: number = Math.max(first, second, third);
  console.log(`The largest number is ${largestNumber}.`);
}

// getLargestNumber(5, -3, 16);
getLargestNumber(-3, -5, -22.5);
